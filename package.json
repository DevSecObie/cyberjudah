{
  "name": "cyberjudah",
  "version": "2.0.0",
  "private": true,
  "description": "The CyberJudah vault: the KJV with the Apocrypha, the study notes, class notes, encyclopedia, handbook of law, precepts and case studies. engine/ builds the data set, site/ renders it.",
  "type": "module",
  "scripts": {
    "notes:fix": "node scripts/link-timestamps.mjs && node scripts/tag-notes.mjs && node scripts/index-scriptures.mjs",
    "notes:lint": "python3 scripts/notes/lint.py",
    "check": "node engine/check.mjs",
    "build": "node engine/build.mjs --out dist",
    "site": "npm --prefix site run build"
  },
  "engines": {
    "node": ">=22"
  }
}
